{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '=== ENV CHECK ===' && echo DATABASE_URL=$DATABASE_URL && echo DJANGO_SETTINGS_MODULE=$DJANGO_SETTINGS_MODULE && python manage.py migrate --noinput && python manage.py loaddata apps/cases/fixtures/court_forms.json && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --timeout 120",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
